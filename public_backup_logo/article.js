const id=new URLSearchParams(location.search).get("id");async function load(){let a=await fetch("/api/articles/"+id).then(r=>r.json());if(a.error)return article.innerHTML="<h1>Introuvable</h1>";article.innerHTML=`<a href="/">← Retour</a><div class="articlehead"><label>${a.breaking?"🚨 BREAKING NEWS • ":""}${esc(a.category)}</label><h1>${esc(a.title)}</h1><p class="meta">Par ${esc(a.author)} • 👁️ ${a.views}</p>${a.image?`<img src="${a.image}">`:""}</div><div class="content">${esc(a.content).replace(/\n/g,"<br>")}</div><div class="reactions">${["👍","❤️","😂","😮","😡"].map(x=>`<button onclick="react('${x}')">${x}</button>`).join("")}</div><section><h2>Commentaires</h2><form id="cf"><textarea id="cc" placeholder="Votre commentaire..." required></textarea><button class="btn red">Commenter</button></form><div>${a.comments.map(c=>`<article class="comment"><b>${esc(c.name)}</b><small>${new Date(c.created_at).toLocaleString("fr-FR")}</small><p>${esc(c.content)}</p></article>`).join("")||"<p>Aucun commentaire.</p>"}</div></section>`;cf.onsubmit=async e=>{e.preventDefault();let r=await fetch("/api/articles/"+id+"/comments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({content:cc.value})});if(r.status===401)return location="/login.html";load()}}async function react(x){let r=await fetch("/api/articles/"+id+"/reactions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({reaction:x})});if(r.status===401)location="/login.html"}function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}load();